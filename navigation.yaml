---
navConfiguration:
- pageId: welcome
  nextOptionList:
  - optionValues:
    - collisionSelected = AUTO
    - collisionSelected = MOTORCYCLE
    nextPageId: vehicle-collision
  - optionValues:
    - collisionSelected = PLANE
    - 'collisionSelected = '
    nextPageId: plane-collision
  backOptionList:
  - optionValues:
    - collisionSelected = AUTO
    - collisionSelected = MOTORCYCLE
    nextPageId: vehicle-collision
  progressStep: 4
  pageNumber: 1
- pageId: reaction
  nextOptionList:
  - optionValues:
    - collisionSelected = FOREST
    - collisionSelected = FOREST
    nextPageId: forest-collision
  - optionValues:
    - collisionSelected = PLANE
    - 'collisionSelected = '
    nextPageId: plane-collision
  backOptionList:
  - optionValues:
    - collisionSelected = CITY
    - collisionSelected = CITY
    nextPageId: vehicle-collision
  progressStep: 2
  pageNumber: 2